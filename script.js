let slider = tns({
    container: ".tiny-slider",
    nav: false,
    items: 1,
    slideBy: "page",
    controlsContainer: '#controls',
    autoplayButtonOutput: false,
    prevButton: '.previous',
    nextButton: '.next',
    autoplay: true
});